import { Box } from "@mui/joy";
import Filters from "../components/agendas/reservations/Filters";

const Reservations = () => {
  return (
    <Box className="main">
      <Filters />
    </Box>
  );
};

export default Reservations;
